#include <stdlib.h>
#include <stdio.h>
/**
*main - prints the name of the executable file
*@argv: the argumnts
*@argc: the number of arguments
*/
int main(int argc, char **argv)
{
printf("%s\n", argv[argc * 0]);
return (0);
}
